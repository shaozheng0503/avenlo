# Avenlo 队友确认清单

> 开发现状：Android 工程已编译通过（三模块 + 9 屏 Compose UI + APK），Mock Server 11 端点跑通，8 屏模拟器截图全验证。
> 以下 3 组问题确认后即可定稿，每项都标了「当前代码的默认值」——确认结果若与默认值不同，改动量都很小。

---

## 一、导航结构（方案 8.1 #1，4 个确认点）

**背景**：设计稿中底部导航只在「我的」屏出现过，且 Tab 标签文字在 .fig 源文件中字符损坏（渲染成「图3」「发联」），「记录」「统计」两个 Tab 没有对应页面设计。

| # | 确认点 | 当前代码默认值 | 若不一致的改动量 |
|---|--------|--------------|----------------|
| 1 | 4 个 Tab 的**准确标签** | 首页 / 记录 / 统计 / 我的 | `Routes.kt` 改 4 个字符串，5 分钟 |
| 2 | **「记录」Tab 落点**：设计稿无此页 | ✅ 已实现 RecordsScreen（时间线：自然日分组 + 卡片复用，QUEUED 自动轮询出卡） | 若有专门设计稿需调整视觉（~半天） |
| 3 | **「统计」Tab 落点**：设计稿无此页 | 复用今日回顾 S5（server 数据驱动） | 若有专门设计稿需新增一屏（1 天） |
| 4 | **「灵感集」「搜索」的入口**（两者是无返回箭头的顶级页，但没有 Tab 位） | 灵感集 = 首页搜索框右侧图标；搜索 = 搜索框本体 | 改 `HomeScreen.kt` 几行 |

**顺带确认**：首页作为 Tab 根页在样机中**没有底部导航**（其他 Tab 页有）——是有意隐藏还是旧版样机？当前代码：首页显示底部导航。

## 二、源文件文字损坏核对（方案 1.3，5 处）

`.fig` 源文件部分文案渲染为错字（字形映射缺陷）。**代码已按语义重建，请队友在 Figma 中逐处核对原始文案**，若有出入改字符串常量即可：

| 位置 | 源文件渲染（错） | 代码采用的重建值 |
|------|----------------|----------------|
| 底部导航 Tab 2 | 「图3」→ | **记录** |
| 底部导航 Tab 3 | 「发联」→ | **统计** |
| 我的页·统计三格 1 | 「本胸己买」→ | **12 灵感** |
| 我的页·统计三格 2 | 「鳳什叉瞋」→ | **86 灵感集** |
| 我的页·统计三格 3 | 「追尾记录」→ | **5 分钟** |

## 三、捕捉屏设计稿评审（方案 8.1 #6）

设计稿中**没有** App 内录音态页面（源文件确认无此屏），Demo 分镜里点「开始记录」后前台的显示由开发自设计。当前实现（深色捕捉语言，与 S7 锁屏视觉同调）：

- `#1C1A18` 深底 + 陶土色呼吸光环 + 麦克风图标
- 状态流：准备 →（轻捏）→ 正在聆听（计时）→（再次轻捏）→ 已存下·AI 整理中 → 灵感已保存（3s 撤销窗）→ 回首页
- 震动语言已按规格站实现：开始轻震 / 保存双震 / 撤销短双震 / 失败急促三连

## 四、捕捉触发手势（新增，已实现待评审）

规格站定义戒指「轻捏 ≥300ms 防误触」。App 内模拟手势当前实现：

- **长按 FAB 300ms**（白色进度环反馈）→ 触发震动 + 直达录音（跳过准备页）
- **短按 FAB** → 进准备页（再点一次开始）——保留兜底
- 交互链：长按 → 直接说话 → 静默 3s 自动保存

若评审希望改为其他交互（如双击、摇一摇、音量键），改动集中在 `HomeScreen.kt` FAB 的 `pointerInput` 块，半小时内可换。

---


**请评审**：视觉方向 OK？还是要出正式设计稿？

---

## 附：怎么验证现状

```bash
# 1) Mock Server（已跑通，种子数据 = .fig 实稿文案）
cd mock-server && ../venv/Scripts/python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
# 浏览器开 http://localhost:8000/docs 可试全部 11 个接口

# 2) APK 装机（产物已产出）
adb install avenlo-android/app/build/outputs/apk/debug/app-debug.apk
# 模拟器 API base = http://10.0.2.2:8000（默认已配）
# 真机需改 avenlo-android/gradle.properties 的 avenlo.api.base 为电脑局域网 IP
```

## 附：关键文件索引

| 内容 | 路径 |
|------|------|
| 开发方案（8.1 阻断项处置进度） | `Avenlo-Android开发方案与设计规划.md` |
| API 契约冻结文档 | `mock-server/API_CONTRACT.md` |
| 导航 Tab 标签（待确认项） | `avenlo-android/app/.../ui/navigation/Routes.kt` |
| 设计 Token（11 色精确值） | `avenlo-android/app/.../ui/theme/Theme.kt` |
