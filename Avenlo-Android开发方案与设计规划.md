# Avenlo Android App 开发方案与设计规划
**Anker 首届黑客松 · Hotfix 队 · 智能录音赛道**

---

## 0. 信息来源与基准说明

本方案信息源经历三轮升级，**当前唯一基准为第三轮**：

1. ~~交互设计规格站 + 6 张预赛海报~~（初版基准，页面清单与部分交互参数仍有效，已被实稿覆盖处以下文 1.4 为准）
2. ~~Figma 导出样机图 3 张 / 7 屏~~（视觉结构基准，已被 .fig 源文件解析覆盖）
3. ✅ **Hackathon.fig 源文件解析**（最终基准）：完整 variables token、全部 UI 文案、170 张内嵌素材、页面存在性确认——第 1.4 节、第 4 节均以此为准

规格站中仍然有效的内容：Pinch 交互参数（≥300ms / 静默 3s / 60s 上限 / 双捏撤销 / 震动语言）、Idea Card V2.1 数据契约、异常兜底流程——这些属于交互与数据层，设计稿不覆盖，继续沿用。

---

## 1. 设计稿分析

### 1.1 页面结构（实稿 8 屏，以 .fig 源文件为准）

| # | 界面 | 核心内容 | 来源确认 |
|---|------|---------|---------|
| S0 | **启动页** | Avenlo 手写体 Logo +「把灵感装进一次轻捏」+ 戒指线稿插画 + 陶土 CTA「开始记录 →」；**无底部导航** | 样机图 + 源文件 |
| S1 | **首页** | 「Hey, Runel ☀️」+「2025年9月20日星期六」+ 右上头像入口；搜索框（占位「搜索灵感、关键词、标签」）；分段 Tab **今天 / 本周 / 更早**；分组「今天·5条›」「本周·5条›」「更早」；Idea Card 列表；**样机中无底部导航（内部不一致，见 1.3）** | 样机图（高倍复核） |
| S2 | **灵感详情**（二级页，带「‹ 返回」+「⋯」） | AI 摘要卡 → 相关想法横滑（右上入口）→ 延展思路折叠列表（3 条）→ 参考资源（右上「立即全部 ›」） | 样机图（高倍复核） |
| S3 | **灵感集** | 页头「灵感集」+「AI自动归类的主题」+ 右上「+」；2×2 卡片（浅主题色底 + 右上**彩色圆形**计数徽标 + 照片在下半部 + 右下白圆箭头）：旅行灵感 12 / 项目构思 5 / 晨间随想 15 / 阅读笔记 7；页尾标语 + 叶子装饰；**无导航、无返回箭头（顶级页特征）** | 样机图（高倍复核） |
| S4 | **搜索/筛选** | 搜索框（占位「搜索灵感、主题或关键词」）+ 筛选图标；常用标签 **#设计 / #摄影 / #待办 / #人物**（四色胶囊）；筛选 全部/今天/本周/收藏；近期搜索 +「清空」；**无导航、无返回箭头** | 样机图（高倍复核） |
| S5 | **今日回顾**（二级页，带「‹ 返回」） | 今日最佳灵感大照片卡（白色金句 + 标签行）→ 意外关联（两卡配对 + 说明文字）→ 明日待延展（2 条带说明）→ 底部提醒开关；**无底部导航** | 样机图（高倍复核） |
| S6 | **我的** | 头像 +「Hey, Runel」+「记录，让生活更有方向」+ 设置齿轮；我的戒指卡（已连接 + **92%** 电量环）；统计三格（**12 / 86 / 5**，标签文字源文件损坏）；设置四项；**唯一画了底部导航的屏** | 样机图（高倍复核） |
| S7 | **捕捉中（锁屏）** | 深色屏 + 实时转写常驻通知（规格站定义） | 规格站（唯一来源） |

底部导航：**4 Tab**，图标为 房子 / 圆点 / 图表 / 人像；标签文字在源文件中**字符损坏**（渲染成「图3」「记录」「发联」「我的」），语义推断为 **首页 / 记录 / 统计 / 我的**。

### 1.3 导航结构与入口关系（15:58 高倍视觉复核，⚠️ 待队伍确认）

**证据链**：

| 屏幕 | 底部导航 | 返回箭头 | 判定 |
|------|---------|---------|------|
| 我的 | ✅ 有（第 4 项高亮） | ✗ | **Tab 根页**（Tab 4） |
| 首页 | ❌ 无（整机完整可见，含 Home 条） | ✗ | 应为 Tab 根页却无导航 → **样机不一致** |
| 灵感集 | ❌ 无 | ✗ | 顶级页，但**无 Tab 归属、无入口定义** |
| 搜索 | ❌ 无 | ✗ | 顶级页，同上 |
| 灵感详情 | ❌ 无 | ✅ 有 | 二级页（从首页卡片进入） |
| 今日回顾 | ❌ 无 | ✅ 有 | 二级页（入口未定义） |
| 启动页 | ❌ 无 | — | 全屏页 |

**必须由队伍确认的 4 件事**：
1. 4 个 Tab 的**准确标签文字**（源文件损坏，推断为 首页/记录/统计/我的——需在 Figma 中核对）
2. **「记录」「统计」两个 Tab 的落点页面**——设计稿中完全不存在这两个页面
3. **「灵感集」「搜索」的入口**——它们是无返回箭头的顶级页，但没有 Tab 位置（首页的哪个元素进入？）
4. **「首页」是否隐藏底部导航**——作为 Tab 根页却没有导航栏，可能是旧版样机或有意隐藏

**⚠️ 严重问题：设计源文件存在文字损坏**
多处文案渲染为错字（「图3」「发联」「本胸己买」「追尾记录」「鳳什叉瞋」等），是系统的字形映射缺陷（同批损坏也存在于 .fig 二进制字符串表中）。**受影响**：底部导航 2 个标签、统计三格 3 个标签、部分卡片标签。**开发不能照抄这些文案**——1.4.2 已按语义重建，但上线前必须让队友在 Figma 中逐处核对。

### 1.2 核心交互流程

**A. 捕捉链路（戒指端，无屏幕）**
```
拇指轻捏(≥300ms) → 单次轻震「已听到」→ 直接说出想法
→ 再次轻捏 或 静默3秒自动结束 → 双震动「已存下」
→ 后台: STT转写 → LLM理解 → 关联 → 延展 → Idea Card 落库
```
关键参数：单条上限 60s；结束后 3 秒内快速双捏=撤销；摘下戒指深度休眠；断连时本地 FIFO 缓存、回连自动同步；无网络时卡片显示「排队中」。

**B. AI 后台四层递进（差异化核心）**
1. 转写（保留原声）→ 2. 理解（分类/命名/标签）→ 3. 关联（发现与旧想法的联系，聚合成灵感集）→ 4. 延展（视角/参考/方向）

**C. 浏览链路（App 端）**
推送通知（"与你 3 天前的想法相关"）→ 首页分段 Tab 浏览 → 详情纵向长页（相关想法回听 → 延展变行动）→ 灵感集沉淀 → 今日回顾收尾 → 搜索/待确认纠错。

**D. 异常兜底**：识别失败→卡片「待确认」+音频保留；AI 排队→灰态「整理中」；关联误判→一键反馈；最近删除保留 30 天。

### 1.4 Figma 实稿确认（15:20 终版，基于 .fig 源文件解析）

已获取并解析 **Hackathon.fig 源文件**（Kiwi 二进制已解压，canvas 数据 2.4MB），以下信息为源文件级精确数据，此前所有"目视近似"标记作废。

**页面资产清单（.fig 内嵌 170 张 PNG 已全部提取至 `figma-assets/`）**：
- 实景照片素材约 15 张可直接用于开发（咖啡馆、城市废墟、摄影师人像、旅途风景等，对应灵感集封面/相关想法缩略图）
- iPhone 样机框、米杏色背景板若干（样机图装饰，开发不需要）
- ⚠️ 第三张样机图「**Avenlo 详情与灵感集三屏样机.png**」存在于源文件中但此前未导出 PNG——**详情页与灵感集页可能有更新版本**，详见 1.4.3

#### 1.4.1 设计 Token（Figma variables 面板截图 + 源文件字符串双重确认）

**Collection 1 · 基础 Token（28 项）**

| 类别 | Token | 值 |
|------|-------|-----|
| 字号 | `font-size-xs / sm / base / lg / xl / 2xl / 3xl` | 12 / 14 / 16 / 18 / 22 / 28 / 36 |
| 间距 | `spacing-1 ~ 6` | 4 / 8 / 16 / 24 / 32 / 48 |
| 圆角 | `radius-sm / md / lg / full` | 4 / 8 / 12 / 999 |
| 颜色 | `primary` | `#BE693F` |
| | `primary-hover` | `#A25935` |
| | `text-primary` | `#2F2C28` |
| | `text-secondary` | `#5A564D` |
| | `text-disabled` | `#B0A294` |
| | `border` | `#DAD0C1` |
| | `bg` | `#FAF8EB` |
| | `surface` | `#FCF9F3` |
| | `success` | `#749378`（鼠尾草绿，已确认） |
| | `warning` | `#D9A55C`（琥珀金，已确认） |
| | `error` | `#C05A4E` |

**源文件中发现但未在截图显示的扩展 Token 组**（设计稿引用了但 variables 面板未展开）：`color/grey|orange|green|violet` 系列、`text/xs~6xl + line-height` 系列、`font/weight-normal~bold`、`radius/sm~2xl`、`color/dbx-*`（Dropbox 风格组件库变量）——说明画布中部分组件用了另一套底层变量，**开发以 Collection 1 的 11 色为准即可**。

#### 1.4.2 UI 文案清单（15:58 高倍视觉复核校正版）

**逐屏实读文案**（错字已按语义重建，⚠️ 标记处源文件字符损坏需队友核对）：

- **启动页**：`把灵感装进一次轻捏` / CTA `开始记录 →`
- **首页**：`Hey, Runel ☀️` / `2025年9月20日星期六` / 搜索占位 `搜索灵感、关键词、标签` / 分段 Tab `今天·本周·更早` / 分组 `今天·5条›`、`本周·5条›`、`更早`
  - 卡片：`关于旅行的灵感` #旅行 #生活 08:22·2分16秒 ｜ `写作素材：时间与记忆` #灵感 #日记 10:38·3分52秒 ｜ `生活片段·咖啡馆` #创作 #美食 11:17·1分36秒 ｜ `城市与人` #旅行 #摄影 前天·5分21秒
  - 图标色轮换：陶土 / 鼠尾草绿 / 淡紫
- **详情页**：`灵感详情` / AI 摘要正文「这条灵感表达了「旅行中的慢生活」的体验。强调在陌生城市中通过观察、聆听和日常邂逅取得新的视角，它可以延展到产品设计、生活方式内容与品牌故事的方向。」
  - `相关想法`（右上入口 ⚠️ 疑为「共5条›」/「立即全部›」，损坏待确认）+ 3 卡：`清晨的露水` 00:22 ｜ `咖啡馆与工作` 09:20 ｜ `山野徒步`
  - `延展思路`（3 条折叠）：①`旅行 × 当地文化体验`「与当地人交流并参与文化活动，打破游客视角的旅行方式」②`旅行与内容创作结合`「将旅行中的体验转化为图文、视频、播客等内容的可能」③`轻量化旅行用品设计`「面向「轻旅行」人群，设计更轻便、更灵活的旅行用品」
  - `参考资源`（右上 `立即全部 ›`）：`The Way We Travel` 等 3 条外链
- **灵感集**：`灵感集` / `AI自动归类的主题` / 计数徽标 `12 / 5 / 15 / 7` / 四卡副标题：`关于远方、路上与不同的生活方式`、`创意想法与产品方向`、`清晨的灵感与碎片`、`书籍、文章与知识沉淀` / 页尾 `收藏生活中的每一个灵感`
- **搜索**：占位 `搜索灵感、主题或关键词` / `常用标签`（`#设计` `#摄影` `#待办` `#人物`）/ `筛选`（`全部`/`今天`/`本周`/`收藏`）/ `近期搜索` + `清空` / 3 条历史：`东东西设计灵感` 00:28 #设计 #旅行灵感 ｜ `人像摄影构图技巧` 07:25 #摄影 #人物 ｜ `明天的待办事项` 07:02 #待办 #生活
- **今日回顾**：`今日回顾` / `2025年9月29日·周一` / `今日最佳灵感` 金句「真正的放松不是躺平，而是把注意力收回来，转回当下的感受。」+ 标签 生活/心情/旅行 / `意外关联`「两个想法在时间与空间上形成了有趣的关联」+ 配对卡 `酒店空间`（度假 × 设计语言 / 研究笔记）↔ `个人成长`（独处时光 × 自我关照 / 思考片段）/ `明日待延展`：①`城市中的自愈疗愈空间`「在高密度城市里创造自然疗愈的角落」②`旅行与工作方式的融合`「是否可以设计一种新的远程工作场景？」/ 底部提醒开关
- **我的**：`Hey, Runel` / `记录，让生活更有方向` / `我的戒指`（`已连接` + `92%`）/ 统计三格 ⚠️ 标签损坏（值 `12 条`、`86 条`、`5 条`）/ 设置四项：`提醒时间`「每日 21:00，回顾你的旅程」、`导出数据`「导出你的记录与灵感」、`AI整理偏好`「个性化你的灵感分类方式」、`帮助与反馈`「我们一直在倾听」
- **字体**：**Microsoft YaHei（Regular/Bold）**——Android 用系统中文字体即可对齐，无需打包

**⚠️ 源文件文字损坏位置汇总**（开发禁用、需队友核对）：底部导航 2 个标签、统计三格 3 个标签、详情页相关想法右上入口、少量卡片标签。

#### 1.4.3 与此前版本的三处差异（以此为准）

1. ~~第三张样机图未导出~~ **已解决**：`figma-export/Root.png` 即「详情与灵感集三屏样机」（灵感详情/灵感集/搜索 三屏），7 屏视觉稿已集齐，4.4 组件规格与样机图逐屏核对无冲突（15:40 视觉复核）。
2. 「录音进行中」应用内屏：源文件中未发现对应文案（无"录音/LIVE/正在捕捉"字样，仅启动页 slogan 含"轻捏"），**确认设计稿中没有这屏**——应用内录音页由开发按 S7 锁屏通知方案自行设计，或沿用规格站屏5
3. 首页用户名确认为「Runel」，问候语日期格式为完整中文长格式

**15:40 视觉复核补充（Root.png 解读后的新发现）**：
- **灵感集页右上角有「+」按钮**——支持手动新建灵感集，此前方案只写了 AI 自动聚合；功能上补一条：S3 需要手动新建入口（P1 简化实现：仅入口+提示，聚仍自动）
- **详情页「延展思路」为折叠列表**（每条带展开箭头），非平铺三行——实现时用可折叠组件
- **详情页「相关想法」区右上为「共 N 条 >」**入口——意味着相关想法有独立列表页（P2 可选，Demo 可只做横滑）
- **搜索页右上角有筛选图标**（滑杆样式）——与筛选 pill 并存
- 样机图中的紫/蓝/粉彩色 chip（#设计 #旅拍 #动力 等）为**浅色底+深色字**马卡龙风，与 11 色 token 不完全对应——按 4.4 降级策略用 `primary`/`success`/`warning` 透明度变体实现

### 1.5 数据契约

**触发事件**（戒指 Plan A/B 或手机模拟器上报）：
```json
{ "type": "capture_end", "ts": 1726382400123,
  "audio": "blob://...", 
  "gestures": [ {"t":0,"ev":"pinch"}, {"t":8400,"ev":"pinch"} ] }
```

**Idea Card V2.1**（AI Pipeline 输出 → App 渲染）：
```json
{ "id":"idea_01h...", "title":"城市公园的声音采集",
  "summary":"…", "transcript":"…", "tags":["声音","城市观察"],
  "status":"ok",              // ok / needs_review / queued
  "related":[{"id":"idea_01g...","title":"旅行中的灵感","relation":"same_collection"}],
  "extension":{"perspectives":[],"references":[],"directions":[]},
  "collection_id":"col_02f...", "audio_url":"...", "created_at":"…" }
```
`status` 五状态与戒指端状态机一一对应，前端按状态渲染「整理中/待确认/灰态」。

---

## 2. 功能模块拆解与优先级排序

优先级原则：**先保证 30 秒 Demo 分镜完整跑通（P0），再叠加差异化亮点（P1），锦上添花靠后（P2/P3）**。

### 2.1 功能模块优先级（P0–P3，按实稿 8 屏校准）

| 优先级 | 模块 | 子功能 | 判定依据 |
|-------|------|--------|---------|
| **P0** | 捕捉触发 | 应用内长按按钮模拟 Pinch（≥300ms 防误触）；震动反馈三模式 | Demo 分镜 0–15s 核心 |
| **P0** | 录音 | 前台服务录音（8–60s）；静默 3s 自动结束；3 秒内撤销 | 链路第一环 |
| **P0** | STT + AI 理解 | 转写保留原声；LLM 摘要/命名/标签 | 四层递进 1–2 层 |
| **P0** | 首页（S1） | 问候头 + 搜索入口 + 今天/本周/全部分段 Tab + 分组卡片列表 | Demo 主舞台 |
| **P0** | 详情页（S2） | AI 摘要卡 + 相关想法 + 延展思路 + 参考资源 纵向长页 + 音频回听 | 「相关想法是灵魂面板」 |
| **P0** | 启动页（S0） | Logo + CTA 直进录音 | 30s 分镜开场 |
| **P1** | AI 关联 | embedding 相似度 → related + 「意外关联」推送文案 | **评审记忆点** |
| **P1** | AI 延展 | 视角/参考/方向三维度 + 参考资源外链 | 差异化第 4 层 |
| **P1** | 灵感集（S3） | 2×2 网格封面 + 条数角标 + AI 聚合 +「+」手动新建入口（简化） | 产品独有概念 |
| **P1** | 我的（S6） | 戒指连接态 + 电量环 + 统计三格 + 设置列表（Mock 数据即可） | Demo 完整性 |
| **P2** | 搜索/筛选（S4） | 标签 chips + 时间筛选 + 近期搜索 | P1 首页搜索框的落地页 |
| **P2** | 今日回顾（S5） | 今日最佳 + 意外关联配对卡 + 明日待延展 + 睡前提醒开关 | 「灵感生长」叙事收尾；时间不够可截图讲解 |
| **P2** | 锁屏捕捉（S7） | 前台服务 + 常驻通知实时转写 | 让评委看见链路在跑 |
| **P2** | 待确认纠错 | needs_review 集中处理；最近删除 30 天 | 信任兜底 |
| **P3** | BLE 真机协议 | GATT 接入实体戒指 | Plan A 路线图 |
| **P3** | 推送/多端同步 | FCM、云端账户 | 赛后迭代 |

**24h 内不做**：账户登录（本地单用户「Runel」写死）、真机 BLE、碎片化适配、全局深色主题。

---

## 3. Android 开发计划（安卓优先）

### 3.1 技术选型

| 层 | 选型 | 理由 |
|----|------|------|
| 语言/UI | **Kotlin + Jetpack Compose + Material 3** | 单人最快出高保真；声明式 UI 与设计稿组件化映射直接 |
| 架构 | MVVM + Repository；UI 层与业务层严格分离 | 为 iOS 扩展预留（见第 5 节） |
| 本地存储 | Room（卡片缓存）+ DataStore（设置） | 断网兜底、冷启动快 |
| 录音 | AudioRecord + Foreground Service（mediaPlayback 类型） | 锁屏/后台持续录制 + 常驻通知 |
| 震动 | VibratorManager 自定义波形 | 实现「轻震/双震/急促三连」震动语言 |
| STT | 云端 API（通义/讯飞/Whisper 三选一，赛前一天定） | 现场网络风险下备好本地 SpeechRecognizer 降级 |
| LLM | OpenAI 兼容接口（Qwen/DeepSeek/GLM 任一） | 摘要/标签/关联/延展 四个 prompt 模板化 |
| 相似度 | 文本 embedding（LLM 厂商同生态） | 关联层核心 |
| 后端 | **单个 FastAPI 服务 + SQLite + 文件目录**（队伍自部署，少政负责） | 黑客松最快；Idea Card JSON 契约即 API 契约 |

### 3.2 阶段划分与时间安排

> 假设：决赛为 **24 小时现场赛**（与预赛材料"24 小时可展示原型"口径一致），赛前有约 **2 周准备期**。若实际赛制不同，按比例伸缩 D-14/D-7 节点即可，24h 现场部分不变。

**阶段〇 · 赛前准备期（D-14 → D-1）：把"能在场外做的"全部做完**

| 时间 | 里程碑 | 交付物 | 负责 |
|------|--------|--------|------|
| D-14~D-10 | **M1 契约冻结** | API 契约（触发事件 + Idea Card V2.1）定稿；LLM/STT 供应商 PoC 通过（准确率、延迟实测） | 少政 + 阿颖 |
| D-14~D-7 | **M2 设计还原** | 8 屏 Compose 静态高保真（Mock 数据驱动），色彩/字体/间距按第 4 节 Token 落地；照片素材从 `figma-assets/` 拷入工程 | 阿颖（张颖评审交互） |
| D-7~D-3 | **M3 链路打通** | 按钮长按 → 录音 → STT → LLM → Idea Card 落库 → 列表展示，端到端真机跑通 | 少政（后端）+ 阿颖（端） |
| D-3~D-1 | **M4 冻结 + 彩排** | 功能冻结只修 bug；30 秒 Demo 分镜全场彩排 ≥3 次；录一份备份演示视频（现场网络挂了用它） | 全组 |
| D-1 | 装备清单 | 测试机 ×2、充电宝、翻页器、热点、备份视频、3D 打印戒指模型 | 王敏鑫核对 |

**阶段一 · 24 小时现场（D-Day）：组装、调优、演示**

| 时段 | 目标 | 内容 |
|------|------|------|
| T+0–2h | 环境就位 | 仓库拉通、密钥配置、真机安装、Mock/真实后端切换开关验证 |
| T+2–8h | **MS1 真实数据贯通** | 首页（S1）/详情（S2）接真实后端；捕捉按钮 → 卡片出现全链路 <60s 延迟达标 |
| T+8–14h | **MS2 差异化上线** | 关联（embedding）+ 延展三维度 + 灵感集聚合；锁屏常驻通知实时转写 |
| T+14–18h | **MS3 演示路径打磨** | 推送文案、震动时序、30s 分镜卡点校准；待确认/纠错补齐（如时间允许） |
| T+18–21h | 压测与兜底 | 连续录 20 条不崩；弱网/断网演练；检查所有异常态文案 |
| T+21–23h | 彩排 + buffer | 完整彩排 2 次；bug 只修 P0；准备 Q&A 弹药（Plan A 路线图、量化数据） |
| T+23–24h | 提交封板 | APK + 备份视频 + 演示账号就位 |

**阶段二 · 赛后迭代（D+1 → D+30，可选）**
- W1：BLE 真机协议接入（GATT profile + 触发事件透传）、账户体系、FCM 推送
- W2–4：多设备同步、灵感集手动编辑、分享卡片图生成、iOS 启动（见第 5 节）

### 3.3 工程结构（为跨平台预留的关键决策）

```
app/                      # Android 壳：Compose UI、震动、前台服务
core-domain/              # 纯 Kotlin：IdeaCard 模型、状态机、用例
core-data/                # Repository、API client（Ktor）、Room
```
- **所有业务规则不写进 Composable**：卡片状态渲染逻辑（ok/needs_review/queued → 文案/灰态）放 domain 层，两端共用。
- API 契约即文档：用 OpenAPI 描述，后端和双端 client 从同一契约生成。

---

## 4. 界面设计规范

> 基准：Figma 实稿视觉语言 —— `bg` 米白底、`primary` 陶土橘棕主色、`success` 鼠尾草绿第二主色、照片封面 + 压暗层；唯一深色屏为捕捉中（S7）。全部色值见 4.1 Token 表。

### 4.1 色彩（源文件级精确值，Figma variables Collection 1）

| Token | 值 | 用途 |
|-------|-----|------|
| `color/primary` | `#BE693F` | 主色：CTA、选中 Tab、波形图标（陶土橘棕） |
| `color/primary-hover` | `#A25935` | 主色按压态 |
| `color/bg` | `#FAF8EB` | 全局页面背景（米白偏暖黄） |
| `color/surface` | `#FCF9F3` | 卡片表面色 |
| `color/text-primary` | `#2F2C28` | 主文本 |
| `color/text-secondary` | `#5A564D` | 次要文本 |
| `color/text-disabled` | `#B0A294` | 禁用/占位文本 |
| `color/border` | `#DAD0C1` | 卡片描边、分隔线 |
| `color/success` | `#749378` | 成功态、已连接点、电量环、延展思路绿标（第二主色） |
| `color/warning` | `#D9A55C` | 警示/待确认、AI 徽标金 |
| `color/error` | `#C05A4E` | 错误（砖红） |
| 照片压暗层 | 黑色 35% 渐变 | 照片封面文字可读性 |
| `capture/dark-bg` | `#1C1A18` | 锁屏捕捉背景（保留规格站定义） |

**字号体系**：12 / 14 / 16 / 18 / 22 / 28 / 36（xs→3xl），正文基准 14sp，页面标题 22sp
**间距体系**：4 / 8 / 16 / 24 / 32 / 48（1→6），页面边距 24，卡片内边距 16
**圆角体系**：4 / 8 / 12 / 999——注意实稿卡片圆角是 **12**（radius-lg），比目视估计的 20 小；chip/按钮用 999 全圆

### 4.2 字体

| 层级 | 规格 | 用途 |
|------|------|------|
| 设计稿字体 | **Microsoft YaHei Regular/Bold**（源文件确认） | Android 直接用系统 sans-serif（Roboto + 中文字体回退）即可，无需打包字体 |
| `3xl` 36sp Bold | 启动页 slogan/大标题 | |
| `2xl` 28sp Bold | 页面大标题（灵感集/今日回顾） | |
| `xl` 22sp Bold | 区块标题、问候语「Hey, Runel」 | |
| `lg` 18sp SemiBold | 卡片标题 | |
| `base` 16sp Regular | 正文、摘要 | |
| `sm` 14sp Regular | 次要正文 | |
| `xs` 12sp Regular | 时间戳、时长、标签 | |
| 数字 | tabular-nums | 统计三格、电量、音频时长 |

### 4.3 间距与形状

- Token 网格：spacing 4/8/16/24/32/48；屏幕边距 **24**；卡片内边距 **16**；卡片间距 **16**；分组间距 **32**。
- 圆角：卡片 **12**（radius-lg）、小元素 **8**（radius-md）、chip/按钮/标签 **999** 全圆、缩略图 **4–8**。
- 卡片描边 `1dp #DAD0C1`，投影极轻或无（实稿靠描边分层）。
- 底部导航 4 Tab，高 64dp，图标线性风格，选中态 `#BE693F`。

### 4.4 核心组件样式（Token 版）

| 组件 | 规格 |
|------|------|
| **启动页（S0）** | `bg` 底 + 柔和色块；「Avenlo」手写体 36–44sp `primary`；slogan「把灵感装进一次轻捏」`xl`；戒指线稿插画；全宽 CTA「开始记录 →」`primary` 底白字 pill（radius 999） |
| **首页 Idea Card（S1）** | `surface` 卡片 radius 12 + `border` 描边；左 44dp 圆角方形波形图标（陶土 `primary` / 鼠尾草绿 `success` / 淡紫三色轮换，紫色无 token 时用 `primary` 透明度变体）；标题 `lg` + 标签 chips（**浅色胶囊、按分类给色**：生活/待办=绿、旅行/心情=粉橙、创作/美食=金、摄影/日记=蓝）+ 两行摘要 `xs` `text-disabled` + 底部元信息行 `xs`「时间 · 时长」+ 右侧 `›` 箭头；分组头「今天 · 5 条 ›」`xl` Bold |
| **分段 Tab（S1）** | **今天 / 本周 / 更早**：容器 `border` 12% 透明底 radius 999，选中项 `primary` 底白字 |
| **详情长页（S2）** | 顶部「‹ 返回」+「灵感详情」`xl` +「⋯」；AI 摘要卡（💡 `warning` 金徽标 + `surface` 卡，正文 `sm` 行高 1.6）；「相关想法」区块头 + 右上入口（`sm` `text-secondary` + `›`）+ 横滑照片卡（约 104dp 宽、radius 8、标题 `sm` + 分类胶囊 + 时长 `xs`）；「延展思路」**折叠列表**（`success`/`warning`/`error` 数字圆标 + `lg` 标题 + `∨` 展开箭头，展开显示一行说明 `xs`）；「参考资源」行（favicon + `sm` 标题 + `›`）+ 区块右上「立即全部 ›」 |
| **灵感集网格（S3）** | 页头「灵感集」`2xl` Bold + 副题「AI自动归类的主题」`sm` `text-secondary` + **右上圆形「+」新建按钮**；2×2 卡片 radius 12，**结构：上半部浅主题色底（绿/桃/金/蓝四色轮换）+ 主题名 `lg` Bold + 副题 `xs`；下半部照片；右上角主题色圆形计数徽标 `xs` 白字；右下白色圆形 → 按钮**；卡片高约 160dp；页尾标语「收藏生活中的每一个灵感」`sm` 居中 + 叶子装饰 |
| **搜索页（S4）** | 搜索框 `surface` 底 radius 999 + 占位「搜索灵感、主题或关键词」`text-disabled` + **右侧筛选图标**；「常用标签」四色胶囊：`#设计`（绿）/ `#摄影`（粉）/ `#待办`（金）/ `#人物`（蓝）；筛选 pill 组（`全部` 选中 `text-primary` 深底白字 / 今天 / 本周 / 收藏）；「近期搜索」区块头 + 右上「清空」；行结构：56dp 照片缩略图 radius 8 + 标题 `sm` + 时间 `xs` + 分类胶囊 + `›` |
| **今日回顾（S5）** | 顶部「‹ 返回」+「今日回顾」`xl` + 日期 `xs`；「今日最佳灵感」区头（金色 ✦ 图标）+ 大照片卡 radius 12（底部压暗 + 白色金句 `lg` + 标签胶囊行）；「意外关联」区头（绿 ↻ 图标）+ 说明文字 `xs` + 两卡并排 + 中间 🔗 图标（卡：照片 + 标题 + 副标题 + 底部小胶囊）；「明日待延展」区头（紫 → 图标）+ 说明 `xs` + 两条行（数字圆标 + `lg` 标题 + `xs` 说明 + `›`）；底部固定行：🌙 图标 + 提醒文案 `sm` + `success` 开关 |
| **我的（S6）** | 头像 64dp 圆 + 「Hey, Runel」`xl` + 副句「记录，让生活更有方向」`xs` `text-secondary` + 右上设置齿轮；「我的戒指」卡（`success` 已连接点 + 戒指图 + 电量环 progress `success` 弧 + `92%` + `›`）；统计三格卡（图标 + `2xl` 数值 + `xs` 单位，`border` 竖线分隔，**12 / 86 / 5**）；设置四行（彩色圆角图标：桃/蓝/紫/绿 + `lg` 标题 + `xs` 副文 + `›`）：提醒时间、导出数据、AI整理偏好、帮助与反馈 |
| **底部导航** | 4 Tab：图标 房子/圆点/图表/人像（标签文字 ⚠️ 待队友核对，推断为 首页/记录/统计/我的），线性图标，选中 `#BE693F`，未选 `text-disabled`；仅在「我的」样机中确认存在 |
| **锁屏捕捉（S7）** | `capture/dark-bg` 深底白字 + LIVE 红点 + 实时转写滚动（规格站定义） |
| **震动语言** | 开始=轻震 1 次（30ms）；结束=双震（30ms+60ms 间隔）；撤销=短双震；失败=急促三连；低电量推迟到下次录音开始时提醒 |

**照片素材**：`figma-assets/` 目录已提取设计稿内全部 170 张图，其中约 15 张实景照片（咖啡馆/城市/旅行/人物）可直接复制进 Android `res/drawable`（或压缩后入 `mipmap`），文件名为 SHA 哈希，开发时按视觉内容挑选取用。

---

## 5. iOS 及跨平台扩展预留方案

### 5.1 分层策略（核心思路：薄客户端 + 重后端）

**真正的可移植性不在 UI 层，而在"逻辑放哪"的决策：**

1. **后端承载一切重逻辑**：STT、LLM 四层递进、embedding 关联、灵感集聚合全在服务端 → iOS 端只是"录音 + 渲染"，天然跨平台。
2. **`core-domain` / `core-data` 用纯 Kotlin 编写**，不依赖 Android SDK → 后续可通过 **Kotlin Multiplatform** 直接编译给 iOS；若不走 KMP，这两层也是清晰的移植说明书。
3. **API 契约（OpenAPI）作为唯一事实源**，双端 client 由契约生成，杜绝两端各自理解。

### 5.2 两条路线对比（赛后再选，不阻塞当前开发）

| | 路线 A：KMP + SwiftUI | 路线 B：Flutter 重写 |
|---|---|---|
| 复用 | domain/data 层 100% 复用，UI 用 SwiftUI 重写 | 仅复用后端与契约 |
| 团队成本 | 需要一名会 Swift 的人；Kotlin 生态延续 | 需要学 Dart/Flutter，但从零开始也快 |
| 上架风险 | 无 | 无 |
| 硬件扩展（BLE 戒指） | 各端写各自的原生 BLE 模块（本来就绕不开原生） | 同左，platform channel 桥接 |
| **推荐** | **推荐 A**（与当前 Kotlin 架构无缝） | 团队若长期多端需求重可考虑 |

### 5.3 平台差异预留清单（当前开发时就注意）

| 差异点 | Android（现在） | iOS（将来） | 预留动作 |
|--------|----------------|------------|---------|
| 推送 | FCM | APNs | 推送由后端统一发起，payload 结构两端通用，只换通道 |
| 后台录音 | Foreground Service + 常驻通知 | 后台 audio mode + Live Activity（锁屏实时转写的天然载体） | 屏5 的通知内容做成独立接口字段，不写死在通知构建里 |
| 震动 | VibratorManager 波形 | Core Haptics（可做到更细腻） | 震动语义表（开始/结束/撤销/失败）放 domain 层，端上只实现映射 |
| 存储 | Room | 移植层或换 SQLite 直连 | Repository 接口隔离，UI 不直接触库 |
| BLE 戒指 | GATT client | CoreBluetooth | 触发事件 JSON 契约两端一致（规格站已定义） |
| 构建发布 | APK 直接装 | TestFlight | 无需预留，赛后事项 |

---

## 6. 风险与对策

| 风险 | 概率 | 对策 |
|------|------|------|
| 现场 STT/LLM 网络不稳 | 中 | 赛前录制完整备份演示视频；本地 SpeechRecognizer 降级开关；手机热点 + 双运营商 |
| LLM 关联质量差（硬凑关联） | 中 | 关联阈值调高宁缺毋滥；Demo 前预灌一组精心调好的种子灵感数据（演示数据 ≠ 造假，链路是真实的） |
| 24h 内 P1 做不完 | 高 | 优先级表就是裁剪清单：P1 中先做「关联」（评审记忆点），灵感集和锁屏可降级为截图讲解 |
| 录音权限/厂商 ROM 杀后台 | 中 | 赛前在演示真机上完成权限引导流程；演示用 Pixel 或主力机，提前关省电策略 |
| 成员并行冲突 | 中 | D-10 冻结 API 契约后前后端完全并行；Mock Server（后端同契约）让 UI 不被阻塞 |

---

## 7. 假设与自评（诚实版）

**本方案依赖的假设（请确认）**：
1. 决赛为 24 小时现场赛制、赛前约 2 周准备期——若实际是"1 周内远程提交"，3.2 节的阶段一需要压缩为"72 小时冲刺"，模块优先级不变。
2. ~~疑似「录音进行中」屏未导出~~ **已解决**：.fig 源文件确认设计稿中无此屏，S7 按锁屏通知方案实现即可。
3. 后端由少政自建 FastAPI 是可行路径；若比赛指定云厂商，迁移成本约半天。
4. 团队 Android 经验以 Compose 技术栈为前提；若更熟 XML View 体系，UI 开发效率预估需上浮 30%。

**自评（.fig 源文件解析后的最终版）**：
- ✅ **设计 Token 全部为源文件精确值**（11 色 + 7 档字号 + 6 档间距 + 4 档圆角），来自 variables 面板截图与 .fig 二进制双重验证；success `#749378` / warning `#D9A55C` 已经队友面板清晰截图最终确认，**全部 11 色无任何待定项**。
- ✅ **全部 UI 文案从源文件提取**（启动/搜索/首页/详情/灵感集/回顾/我的 7 个页面的实际字符串），开发可直接使用，无需臆造占位文案。
- ✅ **照片素材已就位**：.fig 内嵌 170 张 PNG 全部提取到 `figma-assets/`，约 15 张实景照片可直接入包，无需额外准备素材。
- ✅ 确认设计稿中**没有**「录音进行中」应用内屏（源文件无相关文案），此前"疑似缺屏"的疑点解除——录音页按规格站屏5（锁屏通知）方案实现。
- ✅ 页面清单 8 屏、交互流程、优先级、架构排期均已对齐最终信息源；2026-09-20 15:35 完成全文三方对账（方案 ↔ 样机图 ↔ .fig 源文件），已清除全部规格站旧版残留（5 屏清单/3 Tab 底导/详情四 Tab/旧色值引用）。
- ✅ 15:40 二轮视觉复核：Root.png 确认为第三张样机（详情/灵感集/搜索三屏），7 屏视觉稿集齐，逐屏与方案比对后新增 4 个此前遗漏的设计细节（灵感集「+」新建、延展思路折叠列表、相关想法入口、搜索筛选图标）。
- ✅ **15:58 三轮高倍视觉复核**（全图 + 局部 4–7 倍放大逐块判读）：修正了 6 类此前读错的信息——S1 分段 Tab 实为「今天/本周/更早」、卡片时长与标签配对、灵感集计数徽标为上右**彩色圆形**（12/5/15/7）、常用标签为「#设计/#摄影/#待办/#人物」、我的页统计数值 12/86/5 与设置四项副文案、详情页 AI 摘要全文与延展思路三条描述。**同时发现设计源文件存在文字损坏**（导航 2 个标签 + 统计 3 个标签等），已在 1.3 节列明并给出语义重建版。
- ⚠️ 飞书问卷结果文档（预赛材料中的 Form 链接）需登录无法读取——问卷数据未纳入方案验证；如需用调研数据支撑优先级，让队友导出 PDF 或截图发我。
- ⚠️ iOS 部分仍为路线决策与预留清单，符合"安卓优先"题设。

---

## 8. 开发就绪度审核（15:50 全面复核）

### 8.1 开发阻断项处置进度（16:35 更新：#2–#6 已定稿，#1 带默认值开工）

| # | 问题 | 状态 | 实际落地 |
|---|------|------|---------|
| 1 | **导航结构 4 项待确认** | 🟡 **带默认值开工** | 骨架已按建议方案实现：4 Tab = 首页/记录/统计/我的（标签为字符串常量，5 分钟可改）；记录 Tab = S1 列表按日期分组复用、统计 Tab = S5 今日回顾复用、灵感集入口挂首页搜索框右侧图标、搜索 = 搜索框本体。**待队友确认后在 `Routes.kt` 改标签即可** |
| 2 | **Idea Card 契约缺 4 字段** | ✅ **已定稿** | `mock-server/API_CONTRACT.md` 冻结：`POST /collections?name=`、`GET /ideas?query&tag&range`（range∈all/today/week/favorite）、`references` 为 `{title,url}` 数组、CaptureRequest 含 gestures。**11 端点已实现并端到端验证通过**（2026-09-20 16:30） |
| 3 | **STT/LLM 供应商 PoC 后置** | 🟡 建议已采纳待执行 | 方案维持"D-12 双供应商同测"；Mock Server 的 `PROCESS_DELAY_S=3.0` 即模拟整理延迟，后续替换为真实 STT+LLM 调用 |
| 4 | **S5「意外关联」与 Demo 叙事矛盾** | ✅ **已定稿（升 P1）** | 「意外关联」配对卡组件已随 S5 骨架实现（`ReviewScreen.kt`），Demo 分镜保留该叙事点 |
| 5 | **设计源文件文字损坏** | ✅ **已定稿（用重建版）** | 全部 UI 文案采用 1.4.2 语义重建版进代码（`SeedData.kt` / 各 Screen）；损坏位置清单已在 1.3 列明，队友 Figma 核对后如有出入改字符串常量即可 |
| 6 | **App 内录音态 UI 未定义** | ✅ **已定稿（补丁方案实现）** | `CaptureScreen.kt` 按深色捕捉语言自设计：`#1C1A18` 深底 + 呼吸光环麦克风 + 「正在聆听…」+ 实时转写占位 + 底部参数提示；**视觉为开发建议稿，需队伍评审** |

**结论修订**：阻断项 #2–#6 已全部关闭，#1 以「默认值 + 可快速改」策略开工。**工程骨架（app/core-domain/core-data 三模块 + 8 屏 Compose UI + 主题 Token）与 Mock Server（FastAPI，11 端点）已于 2026-09-20 完成首轮实现。**

### 8.2 中等问题（开发中解决，不阻断）

- **空状态设计缺失**：无灵感时的首页、无关联时的详情页——Demo 用预灌数据规避，真实版需要（建议 P2 补两个空态插画）
- **音频回听组件未定稿**：S2 详情页有播放需求，样机图中无播放条样式——用通用「圆钮+进度条」即可，不阻断
- **搜索交互细节未定义**：输入即搜 or 回车搜、历史记录持久化策略——P2 页面，开发时随手定
- **离线队列的状态同步**：本地 Room 与远端状态不一致的合并策略未写——Demo 单用户低频写入，冲突概率≈0，赛后补
- **照片素材版权**：`figma-assets/` 里 170 张图来路未知（队友收集），黑客松 Demo 无风险，若赛后上架需替换为可商用素材

### 8.3 优点确认（审核中验证过的好决策）

- 优先级以 30s Demo 分镜为锚，裁剪逻辑清晰，24h 内可执行性强
- domain/data 层纯 Kotlin + 后端承载重逻辑 → KMP 路线成本最低
- 契约先行（M1 冻结）+ Mock Server 让前后端并行
- Token 全部源文件级精确 + 素材已提取 → UI 还原零猜测
- 风险预案具体（备份视频、本地 STT 降级、种子数据）

### 8.4 结论

**设计侧 100% 就绪，可以立即开工 UI 层**（8 屏中 6 屏有完整视觉依据 + Token + 素材 + 文案）。

**整体进入开发前，需先关闭 8.1 的 4 个问题**——其中 #1（导航归属）是唯一必须由队伍决策的，#2/#3/#4 我可以直接按建议值定稿进方案。关闭后即可进入 M1（契约冻结 + 双供应商 PoC）。
- ⚠️ iOS 部分仍为路线决策与预留清单，符合"安卓优先"题设。

### 8.5 首轮开发实绩（2026-09-20 16:35）

**已交付**（代码位于 `avenlo-android/` 与 `mock-server/`）：

1. **Android 工程骨架**（三模块，为 KMP 预留）：
   - `core-domain`：IdeaCard V2.1 / Collection / DailyReview 模型、Repository 接口、捕捉状态机、震动语义表、交互参数（纯 Kotlin 零 Android 依赖）
   - `core-data`：Ktor API client（契约对齐 Mock Server）+ 内存态 Repository（QUEUED 占位卡断网兜底逻辑）
   - `app`：Compose 主题（11 色 + 7 档字号 + 6 档间距 + 4 档圆角源文件级 Token）、8 屏 NavGraph + 底部导航 4 Tab、S0–S7 全部屏位实现（首页/详情/灵感集/搜索/回顾/我的高保真，捕捉屏为补丁设计稿）
2. **Mock Server**：FastAPI 11 端点，Idea Card V2.1 契约参考实现，预灌 .fig 提取文案的种子数据；捕捉→queued→3s→ok 状态机模拟；契约文档 `API_CONTRACT.md`
3. **环境装配脚本**：`setup_android_env.py`（JDK17 Temurin + cmdline-tools + SDK 34，自动直连/代理切换）

**待完成**（依赖环境下载完毕）：
- ~~JDK17/SDK 下载中 → `gradle wrapper` 生成 → `assembleDebug` 编译验证~~ ✅ **18:31 BUILD SUCCESSFUL**，`app-debug.apk` 19MB 已产出（`avenlo-android/app/build/outputs/apk/debug/`）。环境：JDK17（Temurin 17.0.20.1）+ SDK 34 + Gradle 8.9 + AGP 8.5.2，全程命令行无 IDE
- ~~照片素材从 `figma-assets/` 挑选压入 `res/drawable`~~ ✅ 9 张实景照片（色彩丰富度算法筛选）已入 `res/drawable-nodpi/`（photo_seed_01~09），灵感集/详情相关想法/今日回顾大图卡均已接线
- STT/LLM 真实供应商替换 Mock 的 `PROCESS_DELAY_S`（M1 节点）

**遗留风险**：
- 本机无 Android Studio，若需可视化调试 UI 需后补装；命令行编译不影响交付 APK
- 底部导航「记录/统计」复用 S5/S1 的方案未经队友确认（#1），若队伍另有设计需返工导航层（影响面：`Routes.kt` + `AvenloApp.kt` 两个文件）

### 8.6 第三轮开发实绩（2026-09-20 19:05）

**已交付**：

1. **首页 queued 卡自动轮询**（`HomeScreen.kt` + `IdeaRepositoryImpl.refresh`）
   - 存在 QUEUED 占位卡时每 2s 轮询；`refresh` 改合并逻辑：远端列表 + 保留本地 `idea_local_*` 占位卡（断网不丢卡），`distinctBy` 去重
   - 顺手清理 HomeScreen 死代码（未使用的 `grouped` 变量）
2. **录音静默 3s 自动结束**（`CaptureScreen.kt`）
   - 计时循环改为 200ms 采样：读 `MediaRecorder.maxAmplitude`，连续 3s 低于阈值 800 自动 `stopAndSubmit`；60s 上限与 `totalMs` 驱动的计时显示同步修正
3. **STT/LLM 可插拔框架**（`mock-server/app/pipeline.py`，122 行新增）
   - `SttProvider` / `LlmProvider` Protocol 抽象；MockStt / DashscopeStt（通义 Paraformer 文件识别）/ MockLlm / OpenAICompatibleLlm（OpenAI 兼容接口，默认通义 qwen-plus）
   - 环境变量驱动：`AVENLO_STT_PROVIDER` / `AVENLO_STT_API_KEY` / `AVENLO_LLM_PROVIDER` / `AVENLO_LLM_API_KEY` / `AVENLO_LLM_BASE_URL` / `AVENLO_LLM_MODEL`，无 key 自动回落 mock，**Demo 永远能跑**
   - `run_pipeline` 任一环节异常回落 Mock 文案；mock 模式保留 3s 演示延迟（queued 状态可见），真链路本身有耗时不再叠加
   - 新增 `GET /pipeline` 端点：报告当前供应商（不泄露 key），Demo 前快速查验
4. **回归验证**（19:05）
   - Android：`assembleDebug` BUILD SUCCESSFUL（2m26s），`app-debug.apk` 19MB 更新
   - Mock Server：重启加载新 pipeline；POST /captures → queued → 3s → ok（MockStt+MockLlm 产出标题/摘要/标签）；搜索/回顾/建灵感集端点全过

**接真实供应商的操作**（拿到 key 后零代码改动）：
```bash
# STT：通义 Paraformer
export AVENLO_STT_PROVIDER=dashscope
export AVENLO_STT_API_KEY=sk-xxx
# LLM：任意 OpenAI 兼容接口（通义/DeepSeek/GLM）
export AVENLO_LLM_API_KEY=sk-xxx
export AVENLO_LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1  # 可选
export AVENLO_LLM_MODEL=qwen-plus  # 可选
```

**待办**：
- 真机 `adb install` 验证（等手机连接）
- 队友确认导航 #1（默认值已上线）
- `DashscopeStt` 真实调用未经实测（无 key），音频走本地路径传 server 仅适用于同机 Demo；真机需上传端点或 OSS 中转（M3 议题）

### 8.7 第四/五轮实绩（2026-09-20 19:10 / 19:35）

**真机 Demo 就绪**（第四轮）：
- Mock Server 重绑 `0.0.0.0`（原 127.0.0.1 真机不可达）；`192.168.1.40:8000` 局域网验证通过
- 真机版 APK：`-Pavenlo.api.base=http://192.168.1.40:8000` 重编，BuildConfig 确认烙入（Manifest 已有 `usesCleartextTraffic`）
- `真机Demo指南.md`：3 分钟跑通流程 + 常见问题表 + 换网络重建命令

**pipeline 假服务实测 + Demo 分镜**（第五轮）：
- `tests/fake_openai.py` + `tests/test_pipeline.py`：4 组用例全过——mock 默认模式、OpenAICompatibleLlm 正常解析（markdown 围栏剥离 + 字段映射 + 截断保护）、非 JSON 异常回落、run_pipeline 端到端对假 LLM 产出。`OpenAICompatibleLlm` 从「未经实测」升级为「假服务实测通过」，剩真 key 冒烟
- `30秒Demo分镜脚本.md`：8 镜头分镜表（时间/画面/口播/背后机制四列对齐）+ 备用镜头 + 完整口播词 + 彩排检查清单 + 风险对策表
- `API_CONTRACT.md` 补 `/pipeline` 端点 + 可插拔配置表

**遗留**：真机端到端（等设备）、导航 #1 队友确认、DashscopeStt 真 key 冒烟、现场防火墙规则（无法本机自测）

### 8.8 第十一轮实绩：App↔Server 全链路打通（2026-09-20 20:35）✅

**模拟器 E2E 首次真实跑通 HTTP 提交链路**——此前所有"通过"的验证实际走的都是断网兜底或本地种子（本地种子与 server 种子恰好同 id 同题，掩盖了 GET 解析失败）。本轮修复三个串联 bug：

| # | Bug | 根因 | 修复 |
|---|-----|------|------|
| 1 | POST /captures 报「Kotlin reflection is not available」 | Ktor 2.x 请求侧不自动匹配 Content-Type，`setBody(req)` 无 `contentType(Json)` 时 ContentNegotiation 不接手 | `AvenloApi.submitCapture` 补 `contentType(ContentType.Application.Json)` |
| 2 | 响应报「CardStatus does not contain element with name 'queued'」 | server 小写 `"ok"/"queued"` vs Kotlin 大写枚举，kotlinx.serialization 大小写敏感；**GET /ideas 解析一直静默失败被 runCatching 吞掉** | `CardStatus` 补 `@SerialName` 小写映射 |
| 3 | 首页崩溃 `ArrayIndexOutOfBoundsException: index=-2` | `card.id.hashCode()` 可为负，Kotlin `%` 负数返回负索引；server 新卡 `cap_xxxx` id 首次触发（本地 idea_01~08 hash 恰好全正） | `WaveIconTile` 改 `Math.floorMod` |

**E2E 验证链（模拟器 pixel_6 + android-34，10.0.2.2:8000）**：
冷启动 refresh 拉 server 种子 → 首页 merge（「今天·N条」动态增长）→ 短按 FAB → 轻捏开始 → 录音静默 3s 自动保存 → server 卡数 8→11 → **2s 轮询拉回 server 处理后的完整卡**（标题「（模拟 AI 摘要）…」非「整理中」占位，卡片出现在首页顶部）——**捕捉→上传→AI 整理→回显全闭环**

**配套**：`scripts/verify/` 9 个 adb 验证脚本入库（启动/崩溃/UI dump/网络/E2E 全流程，含设备等待防抖）；commit 15f72ab

**遗留不变**：真机验证（等设备）、导航 #1 队友确认、真 key 冒烟

### 8.9 第十二轮实绩：全屏验证 + STT 真链路加固（2026-09-20 21:35）✅

**8 屏 UI 全部截图验证**（`emulator-screens/04~10`）：首页、详情（AI 摘要/相关想法/延展思路 Tab）、灵感集、搜索（常用标签 + 筛选 chips）、记录、统计（复用今日回顾）、我的（戒指电量环）——**UI 层验证覆盖率 8/8**。音频直传链路确认全通（3 张新卡全带 uploads/ 相对路径 audioUrl）。

**DashscopeStt 修复两处真 bug**（commit 5325f2a）：
| # | 问题 | 影响 | 修复 |
|---|------|------|------|
| 1 | `transcription_url` 结果文件 URL 被直接当 transcript 返回 | 真 key 接入后卡片转写变 URL 字符串 | 补 `_fetch_result`：GET 结果文件 → `transcripts[0].text` |
| 2 | 同步 urllib 调用在 async 函数内，轮询最长 60s | 卡死 FastAPI 事件循环，server 全挂 | 全部 `asyncio.to_thread` 下放线程池（LLM 侧同修） |

**假通义服务实测**（`tests/fake_dashscope.py`）：三段式协议 + FAILED/data:URL 拒收/事件循环不阻塞 5 组用例全过——`DashscopeStt` 升级为「假服务实测通过」，与 LLM 侧同等成色。

**接口限制声明**：通义 `file_urls` 仅收公网 URL（http/https），本地文件 data:base64 会被拒——真机链路音频需 OSS 中转（M3 议题，docstring 已注明）。

**Demo 数据已重置**：server 种子态 8 ideas/4 collections，测试音频清空。正式 Demo 直接可跑。

**遗留**：真机验证、导航 #1 队友确认、真 key 冒烟（三者在「等外部输入」清单）。

### 8.10 第十三轮实绩：全屏接入真实数据源（2026-09-20 22:20）✅

**数据源断层清零**——盘点发现 8 屏中只有首页接了 server，其余屏在吃本地种子。本轮四处修复：

| # | 屏 | 原状 | 修复 |
|---|----|------|------|
| 1 | 搜索 | 纯静态 UI（搜索框是 Text 装饰） | 真实 TextField + 内存过滤（title/summary/tags）+ 筛选 pill + 空态 + 点击进详情 |
| 2 | 今日回顾 | `SeedData.dailyReview` | 接 `/review/today`（扁平→嵌套结构映射在 API 层）|
| 3 | 灵感集 | `SeedData.collections` | 接 `/collections`；**修掉 Tone 枚举大小写坑**（第三处同款问题）|
| 4 | 详情页 | idea_01 硬编码回退种子（覆盖 server 数据） | server 优先（extension/related 非空直接用），断网才回落 |

**枚举大小写坑已成系统性风险**：CardStatus（上轮）、Tone（本轮）连续两处静默解析失败。规则：**任何进 server 契约的 Kotlin 枚举必须显式 @SerialName**。已全量排查（Models.kt/IdeaCard.kt 无其他枚举遗漏）。

**验证判据升级（防种子巧合假阳性）**：用「server 独有特征」做判定——回顾页日期带年份（本地种子格式无年份）、详情页 related=3 条（种子无）、server 版长摘要。截图 11-16 入库。

**已知限制**：adb 无法注入中文，中文搜索正向匹配留待真机。

commit 9f2d9a3。App 全部 8 屏至此统一为「server 优先 + 断网回落本地种子」双态数据源。

### 8.11 第十四轮实绩：四项交互闭环（2026-09-20 22:50）✅

| # | 问题 | 修复 |
|---|------|------|
| 1 | **记录 Tab 路由 bug**——与统计 Tab 都指向 REVIEW（复制粘贴错误），显示回顾内容 | 新增 RecordsScreen（S1 时间线：自然日分组 + HomeIdeaCard 复用），RECORDS 路由接线 |
| 2 | 我的页统计写死 12/86/5 | 接真实数据：灵感数（repo）/ 灵感集数（/collections）/ 录音分钟（durationMs 累计）|
| 3 | 灵感集「+」是 TODO | AlertDialog 新建 → POST /collections → UI 置顶；实测 server 落卡 manual=True |
| 4 | 详情页悬空引用兜底显示错误内容（idea_11/12/13 未落卡） | 空态「这条灵感还在整理中」替代 `?: detailOfIdea01` |

模拟器实测截图 17-20；server 已重置种子态。commit 69404e3。

**至此 App 侧可自主闭环的开发项再次清空**。剩余全部依赖外部输入：真机验证、队友确认（导航 #1 标签 + 手势评审 + 记录/统计 Tab 归属）、通义 key。

### 8.12 第十五轮实绩：related 卡落卡 + 记录 Tab 轮询（2026-09-20 23:55）✅

| # | 问题 | 修复 |
|---|------|------|
| 1 | idea_01 详情页「相关想法 3 条」是悬空引用——idea_11/12/13 只存在于 related 列表，GET /ideas 拉不到，点击进详情只能是空态 | server seed.py + App SeedData.kt **双侧同步补卡**：idea_11 清晨的露水 / idea_12 咖啡馆与工作 / idea_13 山野徒步（11 张种子卡） |
| 2 | 记录 Tab 无 QUEUED 轮询——捕捉提交后记录页不会自动出卡 | RecordsScreen 补 2s 轮询（与 HomeScreen 同款 LaunchedEffect 模式） |

**验证（verify_round15.sh，截图 21/22）**：详情页「相关想法 共3条」→ 点击「清晨的露水」→ 显示 idea_11 **真实卡详情**（AI 摘要「晨间露珠的微观摄影与光影观察」+ 0分22秒 + 延展思路 2 条 + 参考资源），非空态「这条灵感还在整理中」。related 跨卡跳转链路完整闭环。commit 799d62c。

### 8.13 第十六轮实绩：/admin/reset 一键重置（2026-09-21 00:20）✅

**问题**：彩排/演示间数据弄乱（捕捉卡越攒越多、手动灵感集混杂）后只能重启 server 恢复——现场找终端、Ctrl+C、重跑命令太慢。

**修复**：新增 `POST /admin/reset` 端点——锁内清空重建种子态（11 卡 + 4 灵感集），同时清 uploads 音频。实测 12 ideas + 5 collections（含脏数据）→ reset → 11 + 4 全清。文档四处同步（契约/README/彩排清单/真机指南）。commit fc994ab。

**运维价值**：Demo 现场数据污染恢复从「重启 server ~30s」降到「一条 curl 即时」，彩排间反复重演零成本。

### 8.14 第十七轮实绩：verify_all.sh 一键全量回归（2026-09-21 01:10）✅

**问题**：分支约定要求「P0 链路改动必须本机过完整 Demo」，但此前验证靠 15+ 个分散脚本手动逐个跑，无统一 PASS/FAIL 结论；且模拟器验证存在时序竞态（dump 空跑、tap 落点漂移、固定 sleep 边界）导致结果不稳定。

**交付**：`scripts/verify/verify_all.sh`——单命令全量回归，11 项检查（server 健康 / 首页种子 / P0 捕捉→出卡全链路 / 记录 Tab / 我的统计 / 灵感集 / 搜索 / 详情 related / related 跳转），PASS/FAIL 汇总 + 失败项清单，起止自动 `/admin/reset` 保证起态一致与结束恢复。

**调试中拆掉的三类坑**（都已写进脚本注释）：
1. **dump 时序竞态**：冷启动慢于固定 sleep 时 uiautomator dump 空跑 → dump 加 3 次重试
2. **tap 落点漂移**：滚动惯性中 dump 的 bounds 已过时，tap 落到相邻卡 → tap 后校验落点内容，失败回退重试
3. **判据假阴性**：新卡 uuid 首位恰为 0 时 `not startswith('idea_0')` 误判为种子卡 → 改 reset 基数对比（确定性判据）

**新发现**：模拟器连续 4 轮回归后进入 dump 间歇失败态（device offline），重启即恢复——回归跑挂时先查设备状态再怀疑代码。

**最终 11/11 全过**（截图 R01-R09）。commit 47311d7。
